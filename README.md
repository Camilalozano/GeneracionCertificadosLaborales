Instalar librerías:
pip install pandas openpyxl python-docx

La hoja `SECOP_NoEstructurado` del archivo Excel debe incluir la columna `url`. Ese valor se agrega al final de cada certificado como enlace de validación de fuentes oficiales.
